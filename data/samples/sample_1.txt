=== Правило: mvc-missing-antiforgery ===
=== PROMPT ===
На языке csharp найди проблему: $METHOD is a state-changing MVC method that does not validate the antiforgery token or do strict content-type checking. State-changing controller methods should either enforce antiforgery tokens or do strict content-type checking to prevent simple HTTP request types from bypassing CORS preflight controls.
Уязвимый код:
```csharp

```
Безопасный код:
```csharp

```
```csharp
    //ok: mvc-missing-antiforgery
    [HttpPost]
    [ValidateAntiForgeryToken]
    public ...


=== COMPLETION ===
```yaml
rules:
- id: mvc-missing-antiforgery
  languages:
  - csharp
  message: $METHOD is a state-changing MVC method that does not validate the antiforgery token or do strict content-type checking. State-changing controller methods should either enforce antiforgery tokens or do strict content-type checking to prevent simple HTTP request types from bypassing CORS preflight controls.
  patterns:
  - pattern: "[$HTTPMETHOD]\npublic IActionResult $METHOD(...){\n    ...\n}\n"
  - pattern-inside: 'u...

==================================================
