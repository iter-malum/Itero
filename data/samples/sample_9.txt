=== PROMPT ===
На языке csharp найди проблему: Mass assignment or Autobinding vulnerability in code allows an attacker to execute over-posting attacks, which could create a new parameter in the binding request and manipulate the underlying object in the application.
Уязвимый код:
```csharp
    return View("Index", model);
}

public IActionResult Create([Bind(nameof(UserModel.Name))] UserModel model)
{
    context.SaveChanges();
```
Безопасный код:
```csharp
    return View("Index", model);
}

[HttpGet("/")]
public IActionResult Index()
{
```
```csharp
    return NoContent();
}

```

=== COMPLETION ===
```yaml
rules:
- id: mass-assignment
  languages:
  - csharp
  message: Mass assignment or Autobinding vulnerability in code allows an attacker to execute over-posting attacks, which could create a new parameter in the binding request and manipulate the underlying object in the application.
  severity: WARNING
```

