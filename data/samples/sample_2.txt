=== Правило: use_weak_rng_for_keygeneration ===
=== PROMPT ===
На языке csharp найди проблему: You are using an insecure random number generator (RNG) to create a cryptographic key. System.Random must never be used for cryptographic purposes. Use System.Security.Cryptography.RandomNumberGenerator instead.
Уязвимый код:
```csharp
		// ruleid: deeptodoruleid: use_weak_rng_for_keygeneration
		cipher.Key = key;
	}
	
	public void GenerateBadKeyGcm() {
		var rng = new System.Random();
		byte[] key = new byte[16];
		rng.NextBytes(key);
```
Безопасный код:
```cshar...


=== COMPLETION ===
```yaml
rules:
- id: use_weak_rng_for_keygeneration
  languages:
  - csharp
  message: You are using an insecure random number generator (RNG) to create a cryptographic key. System.Random must never be used for cryptographic purposes. Use System.Security.Cryptography.RandomNumberGenerator instead.
  severity: ERROR
```...

==================================================
